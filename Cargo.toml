[package]
name = "wasserxr-demo"
version = "0.1.0"
edition = "2024"

[dependencies]
wasserxr = "0.2.3"
wasserxr-core = {version = "0.2.0", optional = true}

[features]
default = []
static = ["dep:wasserxr-core"]

[lib]
crate-type = ["cdylib"]
